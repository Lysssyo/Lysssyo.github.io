# 今日任务：完成简历

我的工作量：

参与公司内部**大模型应用自动化评测平台**后端研发。平台旨在为各业务线的大模型应用提供**自动化评测**与**效能分析**服务；我主要负责**数据集管理模块**与**实验调度引擎**的开发工作，参与制定接入规范、编写接入文档，支持多业务线的高效接入与定制化评测需求。

- 设计基于 **MVCC 版本区间** 的数据集核心模型，采用 **Copy-on-Write** 实现细粒度变更追踪，结合**零拷贝**机制在单表内支持草稿实时编辑与全量历史回溯；引入 **RocketMQ** 异步构建历史快照，实现查询链路的**冷热分离与降级**，显著提升历史版本读取性能。
- 构建 **OLTP/OLAP 读写分离架构**，针对评测实验结果“高频写入、多维查询”的特征，利用**Binlog 增量订阅** 将实验数据准实时同步至 **Elasticsearch**；将多维组合查询卸载至 ES 集群，有效解决传统数据库在大规模评测结果（JSON Blob）检索时的性能瓶颈。
- 负责**实验失败重跑**模块开发，深入实践**状态机驱动**的**调度器-执行器分离**架构，设计**增量调度**策略；通过扫描主表快照筛选失败 Case，结合 Worker 端**多层幂等校验**机制，实现了失败 Case 稳定重试
- 参与大模型配置质检链路的结果解析工作，针对大模型输出格式不稳定的痛点，设计 **“漏斗式”解析降级策略**；结合JSON 语法自动修复、正则片段提取 、 关键词暴力扫描、解析异常自修正构建五级容错机制，大幅提高结果解析成功率


深入学习平台的调度器、执行器分离的实验调度机制，参与实验失败重跑链路的建设

实现对模型“幻觉”与冗余文本的有效兜底，在保留原始调试信息的同时，将复杂指令下的评测结果解析成功率提升至 **[99%+]**



- 基于 binlog 投递聚合数据文档至ES-》》可以对比字节的实现：用clickhouse，引到开源版与商业版的区别（数据集的内容开源版不可查）
- 失败重跑 -》》》架子抄过来，对比字节的断点恢复
- 大模型结果解析 -》》》 对比字节的解析
- 学习到了实验调度，写后读保护，语义级分布式读写锁

